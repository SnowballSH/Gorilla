fn fib_rec(n)
    if n == 0 0
    else if n == 1 1
    else fib_rec(n - 1) + fib_rec(n - 2)

fn fib_iter(n) {
    a = 0
    b = 1
    counter = 1
    while counter < n {
        h = a
        a = b
        b = a + h
        counter = counter + 1
    }
    b
}

puts(fib_rec(16))
puts(fib_iter(16))
